import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Keyword Sparkle Tool - Country-wise SEO Keyword Finder',
  description: 'Discover powerful keywords and craft optimized titles with our free keyword research tool. Find keywords from Google, YouTube, Amazon, and 10+ platforms.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
